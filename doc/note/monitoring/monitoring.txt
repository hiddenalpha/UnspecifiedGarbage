
## QuickNDirty Overview (linux only)

  && while true ;do echo \
      && printf 'Load  ' && cat /proc/loadavg \
      && echo ' CPU:' && cat /proc/pressure/cpu \
      && echo ' MEM:' && cat /proc/pressure/memory \
      && echo ' IO: ' && cat /proc/pressure/io \
      && echo ' DISC:' && df -h | grep -E '^/' \
      && sleep 2 \
     ;done \


